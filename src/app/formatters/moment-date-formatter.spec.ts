import { MomentDateFormatter } from './moment-date-formatter';

describe('MomentDateFormatter', () => {
  it('should create an instance', () => {
    expect(new MomentDateFormatter()).toBeTruthy();
  });
});
